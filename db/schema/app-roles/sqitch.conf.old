[core]
	engine = pg
	# plan_file = sqitch.plan
	# top_dir = .
# [engine "pg"]
	# target = db:pg:
	# registry = sqitch
	# client = psql
[deploy]
	verify = true
[target "phile_starter"]
	uri = db:pg:phile_starter
[engine "pg"]
	target = phile_starter
